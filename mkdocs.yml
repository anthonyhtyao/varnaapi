# mkdocs.yml

site_name: VARNA API
theme:
  name: "readthedocs"
  # name: "material"

nav:
  - Overview: index.md
  - API: varnaapi.md

plugins:
  - search
  - mkdocstrings:
      default_handler: python
      watch:
        - varnaapi.py


markdown_extensions:
  - footnotes
