# mkdocs.yml

site_name: VARNA API
theme:
  # name: "readthedocs"
  name: "material"

nav:
  - Overview: index.md
  - API: varnaapi.md
  - VARNA: varna.md

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          selection:
            filters:
              - "!^_"
              - "^__init__$"
      watch:
        - varnaapi.py


markdown_extensions:
  - footnotes
