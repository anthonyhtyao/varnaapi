image: python:latest
pages:
  stage: deploy
  only:
    - release-doc
  script:
    - pip install mkdocs-material mkdocstrings
    - PYTHONPATH=src mkdocs build --site-dir public
  artifacts:
    paths:
      - public
